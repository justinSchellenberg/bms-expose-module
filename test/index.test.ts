import * as chai from 'chai';
import ExposeModule from '../index';


describe('Function - ExposeModule', () => {
  const nameSpaceFunc = (a, b) => {
    return a + b;
  };
  
  
  it('should Throw an error if namespace is empty object', () => {
    
    
  })
  
})


