import GetObjectKeys from 'bms-utility-helper-functions/src/GetObjectKeys'

G

export default function ExposeModule(module: Function, namespace: object){
  // if object is empty, just fail and return now.
  
  //initiate a loop, causing it to go... one deeper
  
  // if object contains keys ensure only 1 key
  
  
  
  
}